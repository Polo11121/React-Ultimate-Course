import { Archive, Footer, Header, Main, DarkModeButton } from "components";

export const App = () => (
  <section>
    <DarkModeButton />
    <Header />
    <Main />
    <Archive />
    <Footer />
  </section>
);
